# تحديثات البورتفوليو — التعليمات

تم تعديل هذا المشروع بالكامل ليشمل 3 إضافات:

1. **صورة شخصية** — تمت إضافتها في قسم "About" (نبذة عني)، والملف موجود في:
   `public/photo/me/me.jpeg`
   ⚠️ الصورة الحالية هي صورة placeholder مؤقتة بحروف "MH". **استبدلها بصورتك الحقيقية بنفس الاسم والمسار** (نفس الاسم `me.jpeg` بصيغة JPEG) قبل الرفع.

2. **الوضع الليلي (Dark Mode)** — زر تبديل (أيقونة شمس/قمر) في الهيدر، يحفظ اختيارك في المتصفح (localStorage) ويحترم إعداد نظام تشغيلك تلقائيًا في أول زيارة.

3. **دعم اللغتين العربية والإنجليزية** — زر "AR/EN" في الهيدر، يبدّل كل نصوص الموقع فورًا، ويقلب اتجاه الصفحة بالكامل (RTL) عند اختيار العربية، ويحفظ اختيارك أيضًا.

---

## كيف تدمج هذا مع مشروعك الحالي

هذا الملف المضغوط **لا يحتوي على صور المشاريع الأصلية** (shop-go, tech, e-commerce, ai-core, task-flow, CineScope, EstatePro, magic-corn, favicon) لأنها موجودة بالفعل في مشروعك — تم تجاهلها عمدًا حتى لا يتم استبدالها بالخطأ.

الخطوات:

1. افتح مشروعك الحالي.
2. استبدل مجلد `src` بالكامل بالمجلد المرفق هنا (يحتوي على ملفات جديدة: `context/ThemeContext.jsx`, `context/LanguageContext.jsx`, `data/translations.js`، وتحديثات على باقي المكوّنات).
3. استبدل ملف `index.html` بالملف المرفق (تمت إضافة خط **Cairo** لدعم النصوص العربية بشكل جيد).
4. أضف مجلد `public/photo/me/` مع صورتك الحقيقية بداخله.
5. لاحظ أن حزمة `number-to-words` لم تعد مستخدمة (تم الاستغناء عنها لصالح ترجمة عدد المشاريع بشكل صحيح بالعربي والإنجليزي)، فبإمكانك حذفها من `package.json` عندك إذا رغبت، أو تجاهل الأمر فهي غير مستدعاة في الكود الجديد.
6. شغّل المشروع كالمعتاد:
   ```
   npm install
   npm run dev
   ```

## ملاحظات تقنية

- كل النصوص القابلة للترجمة موجودة في مكان واحد: `src/data/translations.js`.
- ألوان الوضع الليلي معرّفة كمتغيرات CSS في `src/index.css` تحت `[data-theme="dark"]`.
- اتجاه الصفحة (`dir`) واللغة (`lang`) في وسم `<html>` يتغيّران تلقائيًا حسب اللغة المختارة.
